# Default values for dpg-s3-export — the Signals non-PII S3 export CronJob.
#
# Code + image: Blue-Dots-Economy/adhoc-scripts → signals-s3-export/.
# The export manifest under files/manifests/<network>.json is a COPY of the
# reviewed contract in adhoc-scripts (source of truth). Sync it when that
# repo's manifest changes — every run records the manifest sha256 in its S3
# run manifest, so drift is detectable.

image:
  repository: ghcr.io/blue-dots-economy/adhoc-scripts/signals-s3-export
  tag: "main"        # pin to a semver/sha tag per environment overlay
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  automount: false
  name: ""
  # EKS/IRSA: in a provisioned environment this is the SHARED app_sa role (the
  # same one the aggregator worker uses) — the generated global-cloud-values.yaml
  # sets it, along with `name: signals-s3-export`, which must stay listed in the
  # role's trusted service-account subjects.
  # eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT:role/<bb>-<env>-app-sa
  annotations: {}

# ── destination ──────────────────────────────────────────────────────────────
# In a provisioned environment `bucket` is overridden to the SHARED public
# bucket (<bb>-<env>-<account_id>-public). Set a `prefix` when export keys must
# not sit beside other objects at that bucket's root.
s3:
  bucket: signals-export
  prefix: ""             # empty = keys start at <network>/<instance_id>/
  endpointUrl: ""        # empty = real AWS; set for MinIO (e.g. http://minio:9000)
  region: "ap-south-1"
  forcePathStyle: false
  # Static credentials ONLY for non-IRSA setups (MinIO/dev): name of a
  # PRE-CREATED Secret with AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY keys.
  # Takes precedence over `credentials` below.
  existingCredentialsSecret: ""
  # Static credentials rendered BY THIS CHART into a <fullname>-s3 Secret, the
  # same way `databaseUrl` renders <fullname>-db. Use on a cluster with no IRSA
  # (the exporter then authenticates to MinIO/Ceph as an ordinary S3 user).
  #
  # Leave EMPTY on AWS. Empty means no Secret is rendered and no credential env
  # is injected, so the SDK's default chain falls through to the IRSA role on
  # the ServiceAccount — which is what makes IRSA work. Setting these on AWS
  # would override the role with static keys.
  credentials:
    accessKeyId: ""
    secretAccessKey: ""

# ── export behaviour (instance-agnostic) ─────────────────────────────────────
export:
  # 'aggregator_onboarded' (default) or 'all' (explicit opt-in to everything).
  scope: aggregator_onboarded

# Shared DATABASE_URL (read-only role) for all instances — normally injected
# from the generated global-secrets.yaml (output-file builds it from the
# signals_export_ro role + its password). Renders a shared <fullname>-db Secret.
# Per-instance existingDatabaseSecret / databaseUrl override this when set.
databaseUrl: ""

# ── instances — one CronJob + env ConfigMap (+ optional DB Secret) each ─────
# Each Signals instance (own database) exports separately. If two instances
# still share one database, deploy only one entry (two would double-export).
# DB creds resolve per instance: existingDatabaseSecret > databaseUrl >
# the shared top-level databaseUrl above.
instances: []
#  - id: blue_dot_up
#    network: blue_dot
#    schedule: "0 0,12 * * *"        # in cronJob.timeZone
#    suspend: false
#    # Override the shared read-only DATABASE_URL only for a different DB:
#    existingDatabaseSecret: ""
#    databaseUrl: ""

# Networks whose export manifest ConfigMaps to render, from
# files/manifests/<network>.json. Shared across instances of that network.
networks:
  - blue_dot

cronJob:
  timeZone: Asia/Kolkata     # k8s >= 1.27
  concurrencyPolicy: Forbid  # a slow run must not overlap the next slot
  startingDeadlineSeconds: 900
  backoffLimit: 2            # retries safe: runs idempotent per run folder
  activeDeadlineSeconds: 3600
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 5

podAnnotations: {}
podLabels: {}

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 10001
  fsGroup: 10001

securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: false   # job writes NDJSON parts to its work emptyDir
  capabilities:
    drop:
      - ALL

resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    cpu: "1"
    memory: 1Gi

nodeSelector: {}
tolerations: []
affinity: {}
