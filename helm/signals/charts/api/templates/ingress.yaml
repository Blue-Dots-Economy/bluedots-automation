{{- if .Values.ingress.enabled -}}
{{- /*
  Template-driven ingress for the signals API. The routing STRUCTURE is fixed
  here — path /api, strip-path "false" (signals-api serves natively under /api).
  Hosts come from global.publicHosts (defaulting to the single global.publicHost
  — the SAME list the ui ingress uses), so every host that serves the UI also
  serves /api: each per-domain portal is fully same-origin (host/ + host/api),
  no CORS. Other scalars from ingress.* (className / tlsSecret / plugin).

  No cert-manager annotation: the ui subchart owns issuance of the shared host
  cert (ingress.tlsSecret); this object only references it, avoiding a duplicate
  certificate for the same host.
*/ -}}
{{- $fullName := include "dpg-api.fullname" . -}}
{{- $hosts := .Values.global.publicHosts | default (list (required "global.publicHost is required" .Values.global.publicHost)) -}}
{{- $isKong := eq .Values.ingress.className "kong" -}}
{{- /* Kong only accepts these on a route's https_redirect_status_code; anything
   else is rejected by the controller at admission, which silently takes the
   whole route out of service. Fail the render instead. */ -}}
{{- if and $isKong .Values.ingress.forceSSLRedirect -}}
{{- $code := .Values.ingress.httpsRedirectStatusCode | int -}}
{{- if not (has $code (list 426 301 302 307 308)) }}{{ fail (printf "ingress.httpsRedirectStatusCode must be one of 426/301/302/307/308, got %v" $code) }}{{ end -}}
{{- end -}}
{{- /* konghq.com/plugins takes a comma-separated list — combine the existing
   rate-limit-tier reference (if any) with this chart's own HSTS plugin (if
   enabled), rather than clobbering one with the other. */ -}}
{{- $plugins := list -}}
{{- if .Values.ingress.plugin }}{{ $plugins = append $plugins .Values.ingress.plugin }}{{ end -}}
{{- if and $isKong .Values.ingress.hsts.enabled }}{{ $plugins = append $plugins (printf "%s-hsts" $fullName) }}{{ end -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "dpg-api.labels" . | nindent 4 }}
  annotations:
    konghq.com/strip-path: "false"
    {{- if $plugins }}
    konghq.com/plugins: {{ join "," $plugins | quote }}
    {{- end }}
    {{- if and $isKong .Values.ingress.forceSSLRedirect }}
    {{- /* Route-level protocol restriction: only match https, and redirect a
       plain-http request instead of erroring (GITHUB-ISSUES #10). */}}
    konghq.com/protocols: "https"
    konghq.com/https-redirect-status-code: {{ .Values.ingress.httpsRedirectStatusCode | int | quote }}
    {{- end }}
spec:
  {{- with .Values.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ingress.tlsSecret }}
  tls:
    - hosts:
        {{- range $hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .Values.ingress.tlsSecret }}
  {{- end }}
  rules:
    {{- range $hosts }}
    - host: {{ . | quote }}
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $.Values.service.port }}
    {{- end }}
{{- end }}
